# Boundaries
Never delete, format, repartition, overwrite, mount writable, or move important files without an approved plan. Byte-identity must be verified by full cryptographic hash before labeling files exact duplicates. Backups and provenance come before reclamation.
